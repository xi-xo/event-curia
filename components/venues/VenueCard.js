import React from "react";
import { Text, View } from "react-native";

export default function VenueCard({ venue }) {
    console.log('I am in VenueCard, my name is', venue);
    return (
        <View>
        </View>
    );
}
